{
  "name": "electron-covamo",
  "version": "1.0.0",
  "private": true,
  "description": "Yellow document",
  "author": "Daems Kevin",
  "main": "electron/main.cjs",
  "scripts": {
  "dev": "concurrently \"vite\" \"npm run backend-auto\" \"wait-on http://localhost:5173 && electron .\"",
  "backend-auto": "node scripts/start-backend.js",
  "build": "vite build",
  "electron:build": "electron-builder --win --x64",
  "electron:build:mac": "electron-builder --mac"
  },
  "dependencies": {
    "vue": "^3.5.17"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "concurrently": "^9.2.0",
    "cross-env": "^7.0.3",
    "electron": "^37.2.4",
    "electron-builder": "^26.0.12",
    "vite": "^7.0.4",
    "wait-on": "^8.0.4"
  },
  "build": {
    "appId": "be.jouwbedrijf.covamo",
    "productName": "Covamo Electron",
    "asar": true,
    "files": [
      "dist",
      "electron",
      "backend",
      "!**/*.ts",
      "!**/*.map"
    ],
    "extraResources": [
      {
        "from": "backend",
        "to": "backend"
      },
      {
        "from": "venv",
        "to": "venv"
      }
    ],
    "mac": {
      "target": "dmg",
      "category": "public.app-category.productivity",
      "icon": "build/icon.icns"
    },
    "win": {
      "target": "nsis",
      "icon": "build/icon.ico",
      "publish": null
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}
